const checkEmail = require('./checkEmail');
const isRequiredFields = require('./isRequiredFields');
const checkToken = require('./checkToken');

module.exports = {
  checkEmail,
  isRequiredFields,
  checkToken,
};